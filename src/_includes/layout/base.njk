<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta property="og:title" content="{{ metadata.title }}"/>
        <meta property="og:description" content="{{ metadata.description }}"/>
        <meta property="og:author" content="{{ metadata.author }}"/>
        <meta property="og:url" content="https://{{ metadata.base }}{{ page.url }}"/>
        <link rel="stylesheet" href="/index.css"/>
        <title>neavl.ing</title>
    </head>
    <body>
        {% set navigation = collections.all | eleventyNavigation %}
        <header>
            <nav>
                <ul role="navigation">
                    {% for item in navigation %}
                        <li {% if item.url=="/" -%} id="brand" {%- endif -%} {% if item.url==page.url %} class="active" {% endif %}>
                            <a href="{{ item.url }}">{{ item.key }}</a>
                        </li>
                    {% endfor %}
                </ul>
            </nav>
        </header>
        <main>
          {% block main %}
          {% endblock %}
        </main>
        <footer>
            <div>
                <span class="font-courier">Let's connect</span>
                <ul>
                    {% for soc in copy.socials %}
                    <li><a href="{{ soc.link }}" target="_blank">{{ soc.site }}</a></li>
                    {% endfor %}
                </ul>
            </div>
            <picture>
                <source media="(prefers-reduced-motion)" srcset="/assets/img/built-with-eleventy.webp"/>
                <img src="/assets/img/built-with-eleventy.gif" alt="Built with Eleventy"/>
            </picture>
            <span id="copyright">&copy; Matthew Neavling 2025</span>
        </footer>
    </body>
</html>